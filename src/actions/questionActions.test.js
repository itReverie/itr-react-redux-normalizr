import * as questionActions from './questionActions';

describe("questionActions", () => {
  it("should dispatch three actions: questions, parts, suggestions ", () => {

     console.log(questionActions.loadQuestions);
    //act
    // questionActions.loadQuestions().then(result=>{
    //     //assert
    //     expect(result.questions.length).toEqual(4);
    // });
  });
});
